<%= form_with(model: second_demo) do |form| %>
  <% if second_demo.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(second_demo.errors.count, "error") %> prohibited this second_demo from being saved:</h2>

      <ul>
        <% second_demo.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
